# macOS Executable

`scheme-generator` — готовый исполняемый файл для macOS.

## Использование

1. Скачайте файл
2. Дайте права на исполнение:
   ```bash
   chmod +x scheme-generator
   ```
3. Запустите:
   ```bash
   ./scheme-generator <input_file.yaml> [-o <output_file.drawio>]
   ```
