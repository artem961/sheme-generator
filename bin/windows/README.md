# Windows Executable

`scheme-generator.exe` — готовый исполняемый файл для Windows.

## Использование

```cmd
scheme-generator.exe <input_file.yaml> [-o <output_file.drawio>]
```

Просто скачайте файл и запустите его из командной строки или PowerShell.
